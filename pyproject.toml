[project]
name = "csu-global-csc-500"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = []

[project.scripts]
module1 = "modules.module1.arithmetic:main"
module2 = "modules.module2.shopping_cart:main"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"
